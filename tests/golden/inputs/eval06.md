---
patat:
  eval:
    bash:
      command: bash
...

# Slide 1

This file does not have the executable bit set.

That is fine, since none of the code snippets here match an evaluator.

```python
print("Hello, world!")
```
